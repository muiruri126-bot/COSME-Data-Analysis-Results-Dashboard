[build]
  base = "Proposal"
  command = "echo 'Static site - no build needed'"
  publish = "."

# Serve the design platform as the landing page
[[redirects]]
  from = "/"
  to = "/COSME_Phase2_Design_Platform.html"
  status = 200
